{{#if slot.choice}}
  {{#if slot.choice.slug}}
    <span class="text">
      {{#if slot.choice.link}}
        <a {{bindAttr href="slot.choice.link"}} target="_blank">{{slot.choice.original}}</a>
      {{else}}
        {{{slot.choice.original}}
      {{/if}}
    </span>
  {{else}}
    {{view Ember.TextField valueBinding="slot.choice.original"}}
  {{/if}}
{{else}}
  <span class="text">
    -empty-
  <span>
{{/if}}
